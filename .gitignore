# Claude Code 하네스 아티팩트 — 세계 데이터 아님
.claude/scheduled_tasks.lock

# 세션 로컬 설정
.claude/launch.json

# 의존성
engine/node_modules/

# 러너 로컬 상태
engine/.runner.lock
engine/.runner-stats.jsonl
engine/runner.out.log
engine/runner.err.log
engine/runner-state.json
engine/viewer.out.log
engine/viewer.err.log

# 뷰어 접근 토큰 (로컬 비밀 — 커밋 금지)
engine/.viewer-token

# 터널 로그 (로컬 런타임)
engine/tunnel.log
